<html>
    <head>
        <title>Messages</title>
    </head>
    <body>
        <h1>Messages</h1>
        <ul>
            <% for (let i = 0; i < messages.length; i++) { %>
                <p><%= messages[i].text %></p>
                <p><%= messages[i].user %></p>
                <p><%= messages[i].added %></p>
            <% } %>
        </ul>
    </body>
</html>